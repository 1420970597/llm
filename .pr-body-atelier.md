## 为什么

现有控制台按后端流水线堆叠菜单，用户在目标、试制、比较、质量判断和交付之间不断丢失上下文。
本 PR 按 [Discussion #159](https://github.com/1420970597/llm/discussions/159) 的 Atelier 产品方向和
[Issue #160](https://github.com/1420970597/llm/issues/160) 的实施契约，继续把主线迁移到“数据项目工作室”。
正式 Web 入口只有 `3210`；旧版能力不是删除，而是保留深链并提供明确的新入口。

## 做了什么

- 统一 Atelier 全局壳：今日工作、数据项目、方案库、交付库；辅助区承载动态、设置、帮助和兼容索引。
- 统一项目壳：概览、设计、生产、数据、质量、发布；项目、批次、样本、实验和发布 ID 都由 URL 承载。
- 补齐真实跨页跳转：路由元数据成为项目链接、面包屑和导航高亮的单一来源，试制/扩量、样本审阅、质量报告、发布候选不会回到旧壳。
- 把评估和清洗接入 Atelier 原生辅助工作台，复用原有 API 和异步状态；清洗流程按钮指向 `/new`、`/projects`、`/deliveries` 等新入口。
- 在 `/settings/capabilities#admin-governance` 提供真实管理员治理工作区：运行监控、模型服务、结果存储、生成策略、提示词、导出映射和审计。
- 导出映射提供新增/编辑/复制内置映射、默认标记、字段行增删、`{{field}}`/`const:` 表达式和选项 JSON 校验。
- 保留全部 20 个旧 `/console/*` 入口（含别名、任务详情和 6 个管理员入口）；普通用户隐藏管理员组和治理区，空分组不再显示。
- 增加 390px 抽屉导航/响应式布局、锚点深链接滚动、工作区启动时的普通用户成员引导，并更新真实运行截图和 README。

## 真实运行画面

以下截图来自 `docker compose up -d --build` 后的 Chromium，均从 `http://127.0.0.1:3210` 打开（桌面
`1440×1024`，移动 `390×844`）：

| 页面 | 截图 |
| --- | --- |
| 今日工作 | ![今日工作](docs/screenshots/atelier-redesign/today-3210-desktop.png) |
| 项目概览 / 生产蓝图 / 发布版本 | ![项目概览](docs/screenshots/atelier-redesign/overview-3210-desktop.png) ![生产蓝图](docs/screenshots/atelier-redesign/blueprint-3210-desktop.png) ![发布版本](docs/screenshots/atelier-redesign/releases-3210-desktop.png) |
| 评估 / 清洗工作台 | ![评估](docs/screenshots/atelier-redesign/evaluation-3210-desktop.png) ![清洗](docs/screenshots/atelier-redesign/cleaning-3210-desktop.png) |
| 管理治理 / 导出映射编辑器 | ![治理](docs/screenshots/atelier-redesign/admin-governance-3210-desktop.png) ![映射](docs/screenshots/atelier-redesign/admin-mapping-editor-3210-desktop.png) |
| 移动端和角色隔离 | ![今日工作移动端](docs/screenshots/atelier-redesign/today-3210-mobile.png) ![普通用户兼容页](docs/screenshots/atelier-redesign/capabilities-user-3210-mobile.png) |

## 验证

- `npm run build -w apps/web-user`
- `node test/l15_legacy_compatibility.mjs`
- `node test/l15_studio_shell.mjs`
- `node test/l15_studio_entrypoint.mjs`
- `node test/l15_studio_browser_router.mjs`
- `node test/l15_single_web_port.mjs`
- `node scripts/check-docs.mjs`
- Dockerized Go 等价 CI：`gofmt -l`、`go vet ./...`、`go build ./...`、`go test ./...`
- Compose 栈健康：API、Worker、Postgres、Redis、MinIO 和唯一 Web 端口 `3210`
- 浏览器实测：管理员治理 7 个标签、导出映射校验、评估/清洗真实数据、普通用户角色隔离、旧 `/console/tasks`、
  `/console/evaluation`、`/console/cleaning` 深链，以及桌面/移动端无横向溢出。

## 范围边界

本 PR 不关闭 Issue #160。仍需后续完成的生产验收包括真实 provider 业务旅程、断网/重新鉴权和大样本性能基准、
灰度发布与故障回退演练，以及 5–8 名真实用户的任务验收（T29、T33、T34）。这些边界不会用演示数据或静态按钮伪装成已完成。

`Refs #159` `Refs #160`
